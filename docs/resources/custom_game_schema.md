---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "caputchin_custom_game_schema Resource - terraform-provider-caputchin"
subcategory: ""
description: |-
  Per-axis editable-field schema for a custom (non-marketplace) game, declaring the keys customers may override (ADR-0061). Set exactly one of troop_id / site_id. Carries no plan-tier gate (it is metadata describing the shape of presets). Changing scope, game, or axis forces replacement.
---

# caputchin_custom_game_schema (Resource)

Per-axis editable-field schema for a custom (non-marketplace) game, declaring the keys customers may override (ADR-0061). Set exactly one of `troop_id` / `site_id`. Carries no plan-tier gate (it is metadata describing the shape of presets). Changing scope, game, or axis forces replacement.

## Example Usage

```terraform
resource "caputchin_troop" "marketing" {
  name = "marketing"
}

# Declare the editable fields of a custom (non-marketplace) game, per axis.
# The schema describes which keys a preset for this game may set; it carries
# no plan-tier gate (it is metadata). Pair it with caputchin_white_label_preset
# resources that supply the actual values.
resource "caputchin_custom_game_schema" "space_shooter_skin" {
  troop_id = caputchin_troop.marketing.id
  game_id  = "my-org/space-shooter"
  axis     = "skin"
  schema = jsonencode({
    ship_color = "color"
    bg_image   = "image"
  })
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `axis` (String) Override axis: `locale`, `skin`, or `configuration`. Forces replacement.
- `game_id` (String) Game id (e.g. a custom id not in the marketplace) the schema describes. Forces replacement.
- `schema` (String) JSON object declaring the axis's editable fields (use `jsonencode({...})`). Compared semantically.

### Optional

- `site_id` (String) Site id for a per-site schema. Exactly one of troop_id / site_id is required. Forces replacement.
- `troop_id` (String) Troop id for a troop-wide schema. Exactly one of troop_id / site_id is required. Forces replacement.

### Read-Only

- `updated_at` (String) Server timestamp (ISO 8601) of the last write.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import id format: scope|id|game|axis  (scope is troop or site)
terraform import caputchin_custom_game_schema.space_shooter_skin "troop|troop_abc123|my-org/space-shooter|skin"
```
