---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "caputchin_white_label_preset Resource - terraform-provider-caputchin"
subcategory: ""
description: |-
  A single white-label / game-customization preset (ADR-0061). Per-preset granularity: one resource is one preset row. Set exactly one of troop_id (troop-wide baseline) or site_id (per-site override). Omit game_id for a widget-shell preset (Apex tier); set it for a game-axis preset (configuration = Solo+, skin/locale = Alpha+). Changing scope, game, axis, or name forces replacement.
---

# caputchin_white_label_preset (Resource)

A single white-label / game-customization preset (ADR-0061). Per-preset granularity: one resource is one preset row. Set exactly one of `troop_id` (troop-wide baseline) or `site_id` (per-site override). Omit `game_id` for a widget-shell preset (Apex tier); set it for a game-axis preset (configuration = Solo+, skin/locale = Alpha+). Changing scope, game, axis, or name forces replacement.

## Example Usage

```terraform
resource "caputchin_troop" "marketing" {
  name = "marketing"
}

# Widget-shell (white-label) preset, Apex tier. Applies to every site key
# under the troop. Omit game_id for widget-shell presets.
resource "caputchin_white_label_preset" "midnight" {
  troop_id = caputchin_troop.marketing.id
  axis     = "skin"
  name     = "midnight"
  values = jsonencode({
    _theme  = "dark"
    primary = "#0F1810"
  })
}

# Game-axis preset. Tier depends on the axis: configuration is Solo+,
# skin / locale are Alpha+. game_id selects the game (it may contain slashes).
resource "caputchin_white_label_preset" "leaf_hard" {
  troop_id = caputchin_troop.marketing.id
  game_id  = "caputchin/games/leaf"
  axis     = "configuration"
  name     = "hard"
  values = jsonencode({
    difficulty = "hard"
  })
}

# Per-site override. Overrides the troop baseline for one site key only.
resource "caputchin_site_key" "blog" {
  name     = "blog-prod"
  troop_id = caputchin_troop.marketing.id
}

resource "caputchin_white_label_preset" "blog_en" {
  site_id = caputchin_site_key.blog.id
  axis    = "locale"
  name    = "en"
  values = jsonencode({
    _iso       = "en"
    main_title = "Verify you are human"
  })
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `axis` (String) Override axis: `locale`, `skin`, or `configuration`. Forces replacement.
- `name` (String) Preset name. Forces replacement (rename = destroy + recreate).
- `values` (String) JSON object of preset leaf values (use `jsonencode({...})`). Empty / null leaves are not persisted server-side, so omit keys you do not override. Compared semantically, so key order and whitespace never produce a diff.

### Optional

- `game_id` (String) Game id (e.g. `caputchin/games/leaf`) for a game-axis preset. Omit for a widget-shell (white-label) preset. Forces replacement.
- `site_id` (String) Site id for a per-site preset. Exactly one of troop_id / site_id is required. Forces replacement.
- `troop_id` (String) Troop id for a troop-wide preset. Exactly one of troop_id / site_id is required. Forces replacement.

### Read-Only

- `updated_at` (String) Server timestamp (ISO 8601) of the last write.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import id format: scope|id|game|axis|name
# scope is troop or site; game is empty for a widget-shell preset and may
# itself contain slashes for a game-axis preset (the pipe is the delimiter).

# Widget-shell (troop scope):
terraform import caputchin_white_label_preset.midnight "troop|troop_abc123||skin|midnight"

# Game-axis (troop scope, slashy game id):
terraform import caputchin_white_label_preset.leaf_hard "troop|troop_abc123|caputchin/games/leaf|configuration|hard"

# Per-site widget-shell:
terraform import caputchin_white_label_preset.blog_en "site|site_def456||locale|en"
```
