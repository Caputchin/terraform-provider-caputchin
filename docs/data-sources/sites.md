---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "caputchin_sites Data Source - terraform-provider-caputchin"
subcategory: ""
description: |-
  Caputchin site keys the caller can read. Use this for cross-resource lookup (for example, wiring caputchin_site_config against existing sites). The secret is never returned by this data source; it is only available at creation time on the resource. Names-only sites (sites the caller can see exists but cannot read, per the troop-membership permission model) are intentionally excluded from this list because the additional fields would be unknown.
---

# caputchin_sites (Data Source)

Caputchin site keys the caller can read. Use this for cross-resource lookup (for example, wiring `caputchin_site_config` against existing sites). The secret is never returned by this data source; it is only available at creation time on the resource. Names-only sites (sites the caller can see exists but cannot read, per the troop-membership permission model) are intentionally excluded from this list because the additional fields would be unknown.

## Example Usage

```terraform
data "caputchin_sites" "all" {}

output "site_ids" {
  value = [for s in data.caputchin_sites.all.sites : s.id]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `sites` (Attributes List) Visible sites. (see [below for nested schema](#nestedatt--sites))

<a id="nestedatt--sites"></a>
### Nested Schema for `sites`

Read-Only:

- `created_at` (Number) Creation timestamp in milliseconds since the Unix epoch.
- `disabled` (Boolean) Whether the site key is disabled.
- `id` (String) Site identifier.
- `key` (String) Public site key embedded in the widget.
- `name` (String) Human-readable site name.
- `tier` (String) Plan tier inherited from the owning troop.
- `troop_id` (String) Owning troop identifier.
